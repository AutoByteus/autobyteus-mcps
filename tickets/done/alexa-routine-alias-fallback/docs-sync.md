# Docs Sync

Status: Pass

## Documentation Updates

- Updated `alexa-mcp/README.md` with `ALEXA_ROUTINE_EVENT_ALIASES`.

## Rationale

The changed behavior introduces a new public MCP environment variable. The README is the owning local docs surface for Alexa MCP configuration, so documenting the variable there is sufficient.

No broader repository documentation update is required.
